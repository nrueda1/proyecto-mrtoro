const {getConnection} = require("./../model/conexion_db")

const {Encriptar} = require("./../helpers/encrypt")


const Actualizar_admin = async(request,response) => {
    try {
        
    } catch (error) {
        console.error(error)
    }
};

module.exports={
    Actualizar_admin
};